# Critical Prompt 21 検証結果：最終判定・反論耐性

## 妥当性

Prompt 21 は妥当。Issue 候補の重複排除後、最終的な親 Issue または総括文書として実行するのがよい。

## 現時点の反論耐性

無効になりやすい批判：

- `dx` は行列ではない、だけを批判すること。
- 多様体論が前半にない、だけを批判すること。
- 座標自由でない、だけを批判すること。

有効な批判：

- `ds` は 1-form ではない。
- `grad/rot` のベクトル解析表示には `♭/♯` が必要。
- 第10章 Maxwell の行列表示と 2-form 表示の符号は検算が必要。
- 第9章の「任意の座標系」は非直交座標を含むなら過大。
- 章番号・節番号の参照ずれは実害がある。

## 結論

Prompt 21 は最終統合作業で使う。個別 Issue 作成の前に、最強の批判を 8〜12 件へ絞るためのプロンプトとして有効。

